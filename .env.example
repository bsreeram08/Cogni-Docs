# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_LOCATION=asia-south1

# Server Configuration  
UPLOAD_SERVER_PORT=3000
UPLOAD_SERVER_HOST=localhost

# IMPORTANT: Do NOT set GOOGLE_APPLICATION_CREDENTIALS
# Use Application Default Credentials instead:
# 1. Run: gcloud auth application-default login
# 2. Run: unset GOOGLE_APPLICATION_CREDENTIALS (if previously set)
# 3. Verify: gcloud auth list
